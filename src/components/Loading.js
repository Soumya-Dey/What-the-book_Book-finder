import React from "react";

const Loading = () => {
    return (
        <img
            className="loading-anim"
            src="./images/loading.gif"
            alt="loading..."
        ></img>
    );
};

export default Loading;
